#!/usr/bin/env bash

set -euo pipefail

#
# => Languages
#
sudo dnf install -y mise lua luarocks
mise install
mise use -g go@latest

#
# => Developer tools
#
-sudo dnf install -y neovim lazygit
+go install github.com/jesseduffield/lazygit@latest
+sudo dnf install -y neovim

# Docker
# sudo dnf install -y moby-engine docker-buildx docker-compose
# sudo systemctl enable --now docker
# sudo usermod -aG docker "$USER"

#
# => Various tools
#
sudo dnf install -y starship fastfetch lolcat ripgrep fd-find fzf eza pipx zoxide gh

# echo "Docker group membership was added for $USER; log out and back in before using Docker without sudo."
